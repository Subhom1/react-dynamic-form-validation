import React from "react";

export default ({ children }) => {
  return (
    <React.Fragment>
      {/* <Header /> */}
      {children}
      {/* <Footer /> */}
    </React.Fragment>
  );
};
